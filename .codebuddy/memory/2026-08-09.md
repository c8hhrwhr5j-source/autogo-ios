- 2026-08-09 22:25: 从 GitHub 仓库 `c8hhrwhr5j-source/autogo-ios` 拉取代码到本项目。`feat-lua54-engine` 分支远程已不存在，实际克隆了默认分支（master），文件已完整移入 `f:/AutoGo/AutoLua-IOS`。项目包含 AutoLua Swift 源码、Lua 5.4 C 源码、GitHub Actions 工作流、build.sh 及 API 文档。
- 2026-08-09 22:25: 从 GitHub 仓库 `c8hhrwhr5j-source/autogo-ios` 拉取代码到本项目。
- 2026-08-09 23:00: 修复 `AppDelegate.swift` 和多个文件同名导致的 Xcode 编译失败（第一版方案：保留 Core/OCREngine，修改以兼容旧 API）。已推送，构建仍失败 exit 65。
- 2026-08-09 23:30: 第二版修复：发现保留 Core/OCREngine（带 `@available(iOS 13.0, *)`）与 Sources 架构不兼容。改为**恢复 `Sources/OCREngine.swift`**（原始简洁版本，无 @available 限制），**删除 `Core/OCREngine.swift`**。最终文件结构：Sources/（AppDelegate, ScriptEngine, ScriptViewController, ScriptsViewController, SettingsViewController, ShellService, HudOverlay, LogManager, ScreenCapture, TouchController, OCREngine — 共11个swift）、Core/（仅 AppDetector.swift）、Server/（HttpServer.swift）。已推送。
- 2026-08-09 23:55: 第三次修复：`CompileStoryboard` 失败，LaunchScreen.storyboard 不兼容 Xcode 16.2。修复：(1) `targetRuntime="AppleSDK"` → `"iOS.CocoaTouch"`；(2) 文本颜色 `genericGamma22GrayColorSpace`（macOS颜色空间）→ `sRGB`。已推送。
- 2026-08-10 00:05: 第四次修复：`cannot find 'OCREngine' in scope`。OCREngine.swift 使用 `import Vision`，但 project.yml 中未链接 `Vision.framework`，导致 OCREngine 编译失败。新增 Vision.framework 依赖。已推送。
- 2026-08-10 00:15: 第五次修复：OCR 功能暂时无法编译通过，整体删除 OCR 功能。删除 `Sources/OCREngine.swift`，清理 `ShellService.swift`（ocr 命令）、`ScriptEngine.swift`（l_ocr 函数和注册）、`project.yml`（Vision.framework）。后续再单独实现 OCR。已推送。
- 2026-08-10 00:25: 第六次修复：添加 `SWIFT_OBJC_BRIDGING_HEADER` 配置。ScriptEngine.swift 中所有 Lua C API 找不到，因为 project.yml 缺少桥接头文件配置。已推送。
- 2026-08-10 00:45: 第七次修复：Lua 5.4 大量 API 是宏（lua_tointeger, lua_pushcfunction, lua_pop, lua_tostring, lua_pcall, luaL_loadstring, LUA_OK 等），Swift 无法调用 C 宏。创建 `LuaShim.h` C 包装层，重写 `ScriptEngine.swift` 使用 `au_*` 包装函数。修复 deinit 指针转换，移除 unsafeBitCast，清理 SettingsViewController 未使用变量。已推送。
- 2026-08-10 01:00: 第八次修复：(1) `lua_objlen` 也是宏，在 LuaShim.h 添加 `au_objlen` (调用 lua_rawlen)；(2) `findMultiColors`/`findMultiColorsEx` 返回 `[FindMultiColorResult]`，但 `pushPoints` 期望 `[(Int, Int)]`，使用 `.map { ($0.x, $0.y) }` 转换；(3) 顺手修复 AppDetector.swift (unused `app` 变量) 和 HttpServer.swift (var→let) 的编译警告。已推送。
- 2026-08-10 01:10: 第九次修复：`loslib.c` 中 `system()` 在 iOS 上不可用、`tmpnam()` 被废弃。Lua 已内置 `LUA_USE_IOS` 保护（`luaconf.h` 中 `LUA_USE_IOS` 定义 `LUA_USE_POSIX`，`loslib.c` 中 `LUA_USE_IOS` 将 `l_system` 替换为空 stub，`LUA_USE_POSIX` 用 `mkstemp` 替代 `tmpnam`）。只需在 project.yml `GCC_PREPROCESSOR_DEFINITIONS` 中添加 `LUA_USE_IOS=1`。已推送。
- 2026-08-10 01:15: 第十次修复：`BridgeImplementation.m` 中 `#import <IOSurface/IOSurface.h>` — IOSurface 是 iOS 私有框架，SDK 中没有头文件。移除 import，改为：前向声明 `IOSurfaceRef`（即 `CFTypeRef`）、定义 `kIOSurfaceLockReadOnly = 0x00000001`、在 `loadIOMobileFramebuffer()` 中用 `dlopen` 加载 `/System/Library/Frameworks/IOSurface.framework/IOSurface` 并通过 `dlsym` 解析 7 个函数指针（IOSurfaceLock/Unlock/GetWidth/GetHeight/GetBytesPerRow/GetPixelFormat/GetBaseAddress），所有直接调用替换为函数指针调用。已推送。